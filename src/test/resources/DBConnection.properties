# Do not move this file.
# If you move it in an attempt to make the test database configurable,
# you run the risk that a user will accidentally run the tests without
# configuring it and destroy their production database.

hibernate.connection.url=jdbc:postgresql://localhost:5432/tombolo_test
hibernate.connection.username=tombolo_test
hibernate.connection.password=tombolo_test